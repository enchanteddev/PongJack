class Main{
    function void main(){
        var Ball ball;
        var int x;
        let ball = Ball.new(10, 10, 10 ,10, 10);
        let x = ball.getx() + 1;
        do Output.printInt(x);
        do Output.printInt(ball.getx());
        return;
    }
}